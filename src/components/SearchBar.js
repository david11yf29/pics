import React from 'react';

class SeachBar extends React.Component {
    render() {
        console.log(this);
        return (
            <div>
                Search Bar
            </div>
        )
    }
}

export default SeachBar;